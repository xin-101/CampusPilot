spring:
  datasource:
    url: jdbc:mysql://localhost:3306/campuspilot_dev?useSSL=false&serverTimezone=UTC&allowPublicKeyRetrieval=true

logging:
  level:
    com.campuspilot: DEBUG
    org.springframework.security: DEBUG

agent:
  provider: mock