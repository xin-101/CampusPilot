spring:
  datasource:
    url: jdbc:mysql://localhost:3306/campuspilot?useSSL=false&serverTimezone=UTC&allowPublicKeyRetrieval=true
    username: root
    password: root

logging:
  level:
    com.campuspilot: DEBUG
    org.springframework.security: DEBUG

agent:
  provider: mock