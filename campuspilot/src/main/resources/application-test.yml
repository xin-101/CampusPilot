spring:
  datasource:
    url: jdbc:mysql://localhost:3306/campuspilot_test?useSSL=false&serverTimezone=UTC&allowPublicKeyRetrieval=true

logging:
  level:
    com.campuspilot: DEBUG

agent:
  provider: mock